/**
 * This file sets up the tests for the project.
 * It imports the necessary dependencies and configurations for testing.
 *
 * @remarks
 * The `setupDomTests` file is imported to add custom jest matchers for asserting on DOM nodes.
 * The `@testing-library/jest-dom` package is imported to provide additional matchers and utilities for testing.
 *
 * @see {@link https://github.com/testing-library/jest-dom} for more information on `@testing-library/jest-dom`.
 */
import "./setupDomTests";
import "@testing-library/jest-dom";
